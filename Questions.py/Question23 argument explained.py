# Function call for average of 3 numbers:

def average(x,y,z):  # x, y, z are the parameters.
    avg = (x + y + z)/3
    print(avg)
    return avg

average(35,55,95)   # The numbers are my argument.